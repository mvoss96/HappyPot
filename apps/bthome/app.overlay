/* One device, one description: everything lives in the shared .dtsi (which also pulls in the
 * flash map), so this build and the Matter build cannot drift apart. */
#include "../../dts/happypot_hw.dtsi"
